azure-core<=1.19.1
azure-identity>=1.6.0
azure-mgmt-core>= 1.3.0
azure-storage-blob>=12.8.1
msrest>=0.6.21
numpy>=1.21.0
deprecated>=1.2.12
aiohttp>=3.7.0
aiofile>=3.7.2
# We are using this specific version of protobuf for now
# because this is what the backend expects at this time.
# Maybe updated in the future but will be back compat and not
# break users that do not update the qdk version
protobuf>=3.14.0